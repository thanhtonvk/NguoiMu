<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:orientation="vertical"
    tools:context=".views.MainActivity">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        android:gravity="center"
        android:text="Phần mềm hỗ trợ người khiếm thị"
        android:textColor="#9C27B0"
        android:textSize="50sp" />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvTitle"
        android:layout_centerInParent="true"
        android:layout_marginTop="50dp">

        <Button
            android:id="@+id/btnDoDuong"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_alignParentLeft="true"
            android:text="Nhận diện" />


        <Button
            android:id="@+id/btnGoiDien"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_below="@id/btnDoDuong"
            android:layout_alignParentLeft="true"
            android:text="Quay số" />

        <Button
            android:id="@+id/btnGoiDanhBa"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_alignParentRight="true"
            android:text="Gọi trong danh bạ" />

        <ImageButton

            android:id="@+id/btnMic"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_below="@id/btnGoiDanhBa"
            android:layout_alignParentRight="true"
            android:background="#00FFFFFF"
            android:src="@drawable/baseline_mic_24" />
    </RelativeLayout>


</RelativeLayout>